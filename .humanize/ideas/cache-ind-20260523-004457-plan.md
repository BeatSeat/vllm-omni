# IndexTTS2 Implementation Alignment, Emotion Control Validation, and Colab L4 ASR Testing

## Goal Description

Align the vLLM-Omni IndexTTS2 integration (`vllm_omni/model_executor/models/indextts2/`) with the official reference implementation (`cache/index-tts/indextts/`), including full emotion control paths (emo_text, emo_vector, emo_audio). Validate correctness on Google Colab with L4 GPU (authuser=3) by running both offline (`Omni.generate()`) and online (`/v1/audio/speech`) inference, then verifying ASR results via Whisper transcription with `cosine_similarity_text > 0.9` threshold (matching existing buildkite test standards). Output results to `/content/index-tts-outputs/{offline,online}/` with staged progress logging throughout. Commit and push verified code to `feat/index-tts-support`.

**Hard constraints from user:**
- L4 GPU is the target; reduce `gpu_memory_utilization` if needed (forbidden: uploading `cache/` to Colab, running official implementation on L4)
- Emotion control (Qwen Emotion) is in scope
- ASR quality threshold: `cosine_similarity_text > 0.9` between Whisper transcript and target text

## Acceptance Criteria

Following TDD philosophy, each criterion includes positive and negative tests for deterministic verification.

- AC-1: Code alignment between vLLM and official IndexTTS2
  - AC-1.1: All 15 identified component pairs (GPT talker, conformer, perceiver, S2Mel, BigVGAN, semantic codec, CAMPPlus, QwenEmotion, text normalizer, audio I/O, silence removal, pipeline config, model registration, stage processor, deploy YAML) produce functionally equivalent behavior
    - Positive Tests:
      - Same CN text + same real ref audio + same sampling params → both produce recognizable speech (ASR transcript matches target)
      - Same EN text + same real ref audio → both produce recognizable speech
    - Negative Tests:
      - Intentionally mismatched sampling params (e.g., temperature=0 vs 1.0) → outputs differ detectably
      - Missing ref audio → clear error, not silent garbage output
  - AC-1.2: Emotion control paths aligned — emo_text, emo_vector, emo_audio all produce non-default emotion conditioning when QwenEmotion model is available
    - Positive Tests:
      - emo_text with emotional text → emotion vector differs from neutral baseline
      - emo_vector with explicit 8-dim vector → correct matrix lookup applied
      - emo_audio with emotional ref audio + emo_alpha blend → blended output
    - Negative Tests:
      - Invalid emo_vector length (not 8-dim) → validation error
      - emo_alpha outside [0.0, 1.0] → rejected

- AC-2: Offline inference on Colab L4
  - Positive Tests:
    - `Omni.generate()` with CN prompt + real ref audio → valid 22050 Hz mono WAV, non-empty, non-silent
    - `Omni.generate()` with EN prompt + real ref audio → valid 22050 Hz mono WAV
    - Output saved to `/content/index-tts-outputs/offline/output_{id}.wav`
  - Negative Tests:
    - Empty text input → error returned, no crash
    - Missing ref audio → error returned, not silent output

- AC-3: Online serving on Colab L4
  - AC-3.1: Valid requests
    - Positive Tests:
      - POST `/v1/audio/speech` with CN text + base64 ref audio → HTTP 200, valid WAV, 22050 Hz
      - POST `/v1/audio/speech` with EN text → HTTP 200, valid WAV
      - Output saved to `/content/index-tts-outputs/online/output_{id}.wav`
    - Negative Tests:
      - Missing `ref_audio` field → HTTP 4xx error
      - Malformed base64 data URL → HTTP 4xx error
      - Invalid `response_format` → HTTP 4xx error
  - AC-3.2: Emotion API parameters
    - Positive Tests:
      - `emo_text` in extra_params → HTTP 200, audio generated
      - `emo_vector` (8-dim list, values 0-1) → HTTP 200
    - Negative Tests:
      - `emo_vector` with wrong length → HTTP 4xx
      - `emo_alpha` > 1.0 → HTTP 4xx

- AC-4: ASR validation
  - Positive Tests:
    - Offline CN output: `cosine_similarity_text(whisper_transcript, target_text) > 0.9`
    - Offline EN output: `cosine_similarity_text(whisper_transcript, target_text) > 0.9`
    - Online CN output: same threshold
    - Online EN output: same threshold
  - Negative Tests:
    - Audio of pure silence → ASR returns empty/garbage, similarity < 0.9 (correctly flagged as failure)

- AC-5: Output organization
  - Positive Tests:
    - `/content/index-tts-outputs/offline/` contains WAV files + `manifest.json`
    - `/content/index-tts-outputs/online/` contains WAV files + `manifest.json`
    - `manifest.json` includes: prompt text, language, ref audio hash, sampling params, output path, sample rate, duration, ASR transcript, cosine similarity score, git SHA, GPU type
  - Negative Tests:
    - Missing output directory → created automatically, not silent failure

- AC-6: Staged progress logging
  - Positive Tests:
    - Colab output contains timestamped stage markers: model loading, stage 0 start/end, stage 1 start/end, ASR start/end
    - Each stage prints elapsed time
    - Intermediate status: number of mel codes generated (stage 0), mel spectrogram shape (stage 1)
  - Negative Tests:
    - No silent long-running period without output (max 60s between progress updates)

- AC-7: Commit and push
  - Positive Tests:
    - All alignment fixes committed with descriptive message
    - Pushed to `feat/index-tts-support` after Colab validation passes
  - Negative Tests:
    - Debug prints, temporary files, or unformatted code → caught in review, not committed

## Path Boundaries

### Upper Bound (Maximum Acceptable Scope)

Full alignment of all 15 component pairs with emotion control across all 3 modes (emo_text, emo_vector, emo_audio). Both offline and online paths validated on Colab L4 with real reference audio for CN and EN. ASR validation with `cosine_similarity_text > 0.9`. Output manifests with complete metadata. Negative API tests for online serving. Benchmark config entry added to `model_configs.yaml`. Full staged progress logging with per-stage timing and intermediate tensor shapes.

### Lower Bound (Minimum Acceptable Scope)

Core alignment verified (stages 0 and 1 produce valid speech). At least offline mode validated on Colab L4 with ASR threshold met. Emotion control tested with at least one mode (emo_text). Output saved to structured directory. Basic progress logging (stage start/end). Commit and push after validation.

### Allowed Choices

- Can use: Whisper (small or large-v3) for ASR, `cosine_similarity_text` from `tests/helpers/media.py`, real audio from `cache/index-tts-vllm/assets/` or `cache/index-tts-vllm/examples/`, MCP Colab tools for notebook control, `gpu_memory_utilization` values from 0.2 to 0.4 per stage
- Cannot use: Synthetic sine wave as validation reference audio (only for smoke tests), `cache/` directory content uploaded to Colab, official `infer.py`/`infer_v2.py` running on Colab L4, `gpu_memory_utilization > 0.4` per stage (risk OOM)

## Feasibility Hints and Suggestions

> **Note**: This section is for reference and understanding only.

### Conceptual Approach

```
1. ALIGN: Diff official vs vLLM component by component
   - For each of 15 pairs: compare class structure, forward() signature, weight loading, dtype handling
   - Fix divergences, especially in emotion paths (currently untested)

2. COLAB HARNESS: Build test script (not notebook cells) that:
   a. Install deps from git repo (pip install -e .)
   b. Download model (IndexTeam/IndexTTS-2 from HuggingFace)
   c. Run offline: Omni.generate() with real ref audio
   d. Run online: start vllm serve, POST to /v1/audio/speech
   e. For each output: run Whisper ASR, compute cosine_similarity_text
   f. Save outputs + manifest.json to /content/index-tts-outputs/

3. PROGRESS: Wrap each stage with print(f"[{timestamp}] [{stage}] {status}")
   - Model loading, preprocessing, stage 0, stage 1, ASR, done

4. VALIDATE: Check cosine_similarity_text > 0.9 for all outputs
   - If fail: save failure details in manifest, continue other tests

5. COMMIT: After all validations pass, commit + push
```

### Relevant References

- `vllm_omni/model_executor/models/indextts2/` — vLLM integration code
- `cache/index-tts/indextts/infer_v2.py` — Official reference implementation
- `cache/index-tts-vllm/assets/` — Real reference audio files
- `cache/index-tts-vllm/examples/` — Voice sample files (01-12)
- `tests/helpers/media.py` — `cosine_similarity_text()`, `convert_audio_file_to_text()`
- `tests/helpers/assertions.py` — `assert_audio_speech_response()` with 0.9 threshold
- `vllm_omni/deploy/indextts2.yaml` — Deploy config (stage GPU allocation)
- `vllm_omni/entrypoints/openai/serving_speech.py` — Online serving + emotion param validation
- `vllm_omni/model_executor/models/indextts2/preprocess_utils.py` — `load_qwen_emotion()`
- `vllm_omni/benchmarks/data_modules/seed_tts_eval.py` — Seed-TTS eval framework
- `benchmarks/tts/model_configs.yaml` — Benchmark model registry (needs IndexTTS2 entry)
- `examples/offline_inference/text_to_speech/indextts2/end2end.py` — Offline example
- `examples/online_serving/text_to_speech/indextts2/speech_client.py` — Online example

## Dependencies and Sequence

### Milestones

1. **Code Alignment**: Verify and fix all component divergences
   - Systematic diff of 15 component pairs
   - Fix emotion control paths (emo_text, emo_vector, emo_audio)
   - Verify sampling params match between official and vLLM
   - Add benchmark config entry for IndexTTS2

2. **Colab Test Harness**: Build and deploy test infrastructure
   - Create Colab-compatible test script with staged progress logging
   - Configure L4 GPU via MCP (authuser=3, accelerator=L4)
   - Set up output directory structure and manifest generation
   - Tune `gpu_memory_utilization` for L4 (start 0.3+0.3, increase if stable)

3. **Validation Execution**: Run tests and verify ASR results
   - Run offline inference (CN + EN) with real reference audio
   - Run online serving (CN + EN) with real reference audio
   - Test emotion control modes (emo_text at minimum, emo_vector and emo_audio if model available)
   - Compute ASR cosine similarity, verify > 0.9
   - Run negative API tests for online serving

4. **Commit and Ship**: Package results and push
   - Review all changes for debug artifacts
   - Commit with descriptive message
   - Push to `feat/index-tts-support`

Milestone 1 must complete before Milestone 2. Milestone 2 must complete before Milestone 3. Milestone 4 depends on Milestone 3 passing.

## Task Breakdown

| Task ID | Description | Target AC | Tag | Depends On |
|---------|-------------|-----------|-----|------------|
| task1 | Systematic diff of all 15 component pairs between official and vLLM, document divergences | AC-1.1 | analyze | - |
| task2 | Fix identified alignment gaps in model code (talker, s2mel, preprocessing) | AC-1.1 | coding | task1 |
| task3 | Verify and fix emotion control paths (emo_text, emo_vector, emo_audio) in talker and serving layer | AC-1.2 | coding | task1 |
| task4 | Add IndexTTS2 entry to benchmarks/tts/model_configs.yaml | AC-1.1 | coding | task1 |
| task5 | Create Colab test script with offline inference + staged progress logging | AC-2, AC-6 | coding | task2, task3 |
| task6 | Create Colab test script with online serving + negative API tests + staged progress logging | AC-3, AC-6 | coding | task2, task3 |
| task7 | Integrate ASR validation (Whisper + cosine_similarity_text) into both test scripts | AC-4 | coding | task5, task6 |
| task8 | Implement output directory structure and manifest.json generation | AC-5 | coding | task5, task6 |
| task9 | Configure Colab: L4 GPU, authuser=3, install deps, download model, tune gpu_memory_utilization | AC-2, AC-3 | coding | task5, task6 |
| task10 | Execute offline tests on Colab L4 with real ref audio, verify ASR > 0.9 | AC-2, AC-4 | coding | task7, task8, task9 |
| task11 | Execute online tests on Colab L4 with real ref audio + emotion modes, verify ASR > 0.9 | AC-3, AC-4 | coding | task7, task8, task9 |
| task12 | Review results, verify output manifests, validate all ACs | AC-1 to AC-6 | analyze | task10, task11 |
| task13 | Commit aligned code and push to feat/index-tts-support | AC-7 | coding | task12 |

## Claude-Codex Deliberation

### Agreements
- Real reference audio required for meaningful validation (no sine waves)
- ASR threshold of `cosine_similarity_text > 0.9` matches existing buildkite test standards
- Push-after-validation is the correct ordering
- L4 memory is tight but feasible by reducing `gpu_memory_utilization`
- Sample rate is consistently 22050 Hz (not a gap)
- Official implementation's training-only modules (dac, openvoice, melvqgan) are intentionally excluded from vLLM
- Benchmark CLI needs IndexTTS2 config entry in model_configs.yaml
- Emotion control fallback (return None when model missing) needs testing
- Negative API tests are required for online serving validation
- ASR model memory should be separated from TTS model memory (run ASR after TTS, or use Whisper small)

### Resolved Disagreements
- **AC-1 scope**: Codex argued WER comparison alone is insufficient for parity. Resolution: AC-1 now includes duration, sample rate, silence ratio, and ASR transcript comparison. Full perceptual/speaker similarity is deferred (optional improvement, not blocking).
- **Identical inputs definition**: Codex requested explicit param subset. Resolution: Plan specifies same text, same real ref audio, same sampling params (temperature, top_p, top_k, max_tokens, repetition_penalty), same seed where supported.
- **L4 memory strategy**: Codex flagged 0.4+0.4 as optimistic. Resolution: Plan starts with 0.3+0.3 and increases if stable. Sequential ASR (after TTS unload) to avoid memory pressure.
- **Commit as AC**: Codex noted commit/push is procedural. Resolution: AC-7 retained but marked as dependent on all other ACs passing first.
- **Manifest schema**: Codex requested upfront specification. Resolution: AC-5 now specifies exact fields (prompt text, language, ref audio hash, sampling params, output path, sr, duration, ASR transcript, cosine similarity, git SHA, GPU type).

### Convergence Status
- Final Status: `converged`
- Rounds: 1 (all REQUIRED_CHANGES addressed in single revision, no material disagreements remaining)

## Pending User Decisions

All user decisions have been resolved during Phase 6:

- DEC-1: ASR threshold standard
  - Decision: Use `cosine_similarity_text > 0.9` matching buildkite tests
  - Decision Status: `RESOLVED — user confirmed`

- DEC-2: Emotion control scope
  - Decision: Include all emotion modes (emo_text, emo_vector, emo_audio)
  - Decision Status: `RESOLVED — user confirmed "包含"`

- DEC-3: L4 memory fallback
  - Decision: Reduce `gpu_memory_utilization`, forbidden to upload cache/ or run official on L4
  - Decision Status: `RESOLVED — user confirmed "降低gpu_memory_utilization"`

## Implementation Notes

### Code Style Requirements
- Implementation code and comments must NOT contain plan-specific terminology such as "AC-", "Milestone", "Step", "Phase", or similar workflow markers
- These terms are for plan documentation only, not for the resulting codebase
- Use descriptive, domain-appropriate naming in code instead
- Match existing codebase comment language (English for code comments in vllm_omni/)

### Memory Management on L4
- Start with `gpu_memory_utilization: 0.3` for both stages (0.3 * 24GB = ~7.2GB per stage, ~14.4GB total)
- After TTS generation completes for a batch, unload model / kill server before running ASR
- Use Whisper `small` model (not `large-v3`) on Colab L4 to save memory
- Monitor VRAM usage with `nvidia-smi` between stages

### Reference Audio Selection
- Use real WAV files from `cache/index-tts-vllm/assets/` or `cache/index-tts-vllm/examples/`
- These files must NOT be uploaded to Colab; instead, the Colab test script should download them from the git repo after clone
- Select at least one CN speaker and one EN speaker for validation

### Progress Logging Format
```
[2026-05-23 12:00:01] [SETUP] Installing dependencies...
[2026-05-23 12:02:30] [SETUP] Model downloaded, GPU: L4 24GB
[2026-05-23 12:02:31] [OFFLINE] Starting offline inference...
[2026-05-23 12:02:32] [OFFLINE][Stage-0] GPT AR talker starting, text="你好世界"
[2026-05-23 12:02:45] [OFFLINE][Stage-0] Complete: 1200 mel codes generated (13.2s)
[2026-05-23 12:02:46] [OFFLINE][Stage-1] S2Mel + BigVGAN starting, mel_codes=1200
[2026-05-23 12:02:52] [OFFLINE][Stage-1] Complete: wav shape=(48000,) sr=22050 (6.1s)
[2026-05-23 12:02:53] [OFFLINE][ASR] Running Whisper on output...
[2026-05-23 12:02:58] [OFFLINE][ASR] Transcript: "你好世界", similarity: 0.95 ✓
```

--- Original Design Draft Start ---

# IndexTTS2 Official Implementation Alignment And Colab Validation Pipeline

## Original Idea

Align with the official IndexTTS2 implementation under cache/index-tts as closely as possible, then commit+push. Use Colab with authuser=3 and L4 GPU to test whether ASR results are correct (both offline and online modes). Generate results to /content/index-tts-outputs on Colab, separated by online and offline directories. Must include appropriate intermediate output, and scripts should print staged progress information during execution so it is easy to see where things are stuck.

## Primary Direction: Official Implementation Gap Analysis

### Rationale

Focus on systematically comparing current code against cache/index-tts official IndexTTS2 to identify all divergences that need alignment — this is the foundational prerequisite before any testing or validation work can be meaningful.

### Approach Summary

Systematically align the current vLLM-Omni IndexTTS2 integration (`vllm_omni/model_executor/models/indextts2/`) with the official IndexTTS2 reference implementation in `cache/index-tts/indextts/` by identifying and bridging implementation gaps across model stages, utilities, and test infrastructure.

**Core mechanism:**
1. **Stage 0 (GPT AR Talker):** Review vLLM-native PagedAttention-based implementation against official UnifiedVoice model, ensuring equivalent functionality for text→mel-code prediction and hidden-state accumulation for latent conditioning.
2. **Stage 1 (S2Mel + BigVGAN):** Cross-validate S2Mel flow-matching decoder against official MyModel/CFM implementation, ensure BigVGAN vocoder parity, and verify mel-spectrogram synthesis pipeline.
3. **Utilities & Preprocessing:** Align audio I/O, semantic codec loading, CAMPPlus speaker embeddings, Qwen emotion conditioning, and text normalization/tokenization with official implementations.
4. **Pipeline Definition:** Ensure two-stage non-streaming pipeline config (Stage 0→1 with non-async execution) matches official infer_v2.py behavior.
5. **Testing & Validation:** Implement offline (end2end.py) and online serving (speech_client.py) test patterns with step-wise progress logging for ASR result validation.

### Objective Evidence

- **GPT AR Talker Stage:** Official `cache/index-tts/indextts/gpt/model_v2.py` (UnifiedVoice + GPT2InferenceModel with model-parallel support) vs current `vllm_omni/model_executor/models/indextts2/indextts2_talker.py` (vLLM-native GPT2 with PagedAttention). Need to verify custom embeddings (text_pos_emb), conformer encoder, perceiver resampler alignment.
- **Text Conditioning (Conformer + Perceiver):** Both have `gpt/conformer_encoder.py`, `gpt/perceiver.py` with identical or near-identical signatures. Official infer_v2.py loads via UnifiedVoice; vLLM loads via ConformerEncoder (line 39) and PerceiverResampler (line 41).
- **S2Mel Decoder (Stage 1):** Official `cache/index-tts/indextts/s2mel/modules/` and current `vllm_omni/model_executor/models/indextts2/s2mel/modules/` share 7 core modules. Recent git commits (f6066f48, 65c25aad) show active alignment work on float32 CFM and dtype casting.
- **BigVGAN Vocoder:** Both call `BigVGAN.from_pretrained()` + `.remove_weight_norm()` + `.eval()` identically. vLLM adds upstream compatibility fix (`patch_bigvgan_compat`) for huggingface_hub>=1.0 not in official.
- **Semantic Codec (RepCodec):** Both use RepCodec with identical config keys (codebook_size, hidden_size, codebook_dim) at configuration_indextts2.py:63-70. vLLM centralizes loading to preprocess_utils.py for reuse.
- **Speaker/Emotion Conditioning:** Both use identical CAMPPlus DTDNN architecture (feat_dim=80, embedding_size=192). vLLM's load_qwen_emotion lazy-loads from model_path/qwen0.6bemo4-merge.
- **Text Normalization & Tokenization:** Both define identical TextNormalizer.__init__ and char_rep_map in utils/front.py. Glossary path in config matches official glossary.yaml loading.
- **Audio I/O & Feature Extraction:** Official uses librosa.load() + torchaudio + soundfile fallbacks. vLLM's preprocess_utils.py implements load_reference_audio() with torchaudio→soundfile fallback chain, additionally accepting pre-loaded (wav_list, sr) tuple for online serving.
- **Silence Removal & Code Trimming:** Both detect stop_mel_token via nonzero(), shrink consecutive silent_token up to max_consecutive=30, and pad to max_len identically. stage_input_processors/indextts2.py:13-87 matches infer_v2.py:228-283.
- **Pipeline Configuration:** vLLM pipeline.py defines INDEXTTS2_PIPELINE with Stage 0 (LLM_AR) → Stage 1 (LLM_GENERATION), async_chunk=false, matching official infer_v2.py's collect-all-then-decode pattern.
- **Test Infrastructure (Offline):** examples/offline_inference/text_to_speech/indextts2/end2end.py and tests/e2e/offline_inference/test_indextts2_expansion.py both build requests with additional_information={"text": [...], "voice": [...]} pattern.
- **Test Infrastructure (Online):** examples/online_serving/text_to_speech/indextts2/speech_client.py and tests/e2e/online_serving/test_indextts2_expansion.py both encode reference audio as data:audio/wav;base64,... for /v1/audio/speech endpoint.
- **Model Registration:** Pipeline registry at config/pipeline_registry.py registers ("indextts2", "vllm_omni.model_executor.models.indextts2.pipeline", "INDEXTTS2_PIPELINE"). Stage processor registered via sync_process_input_func.
- **S2Mel Module Scope:** vLLM explicitly excludes training-only modules (dac, openvoice training, melvqgan). Official has ~330 .py files in s2mel; vLLM has ~10 — intentional inference-only scope reduction, not a gap.
- **Recent Bug Fixes:** Git history shows 20+ commits aligning vLLM integration with official behavior (float32 CFM, model_dtype casting, latent handling, S_ref embedding fixes).

### Known Risks

- **Qwen Emotion Model Fallback:** vLLM's load_qwen_emotion() logs warning and returns (None, None) if not found, silently disabling emotion conditioning. Test suite doesn't validate emotion audio path.
- **CUDA Kernel Availability:** BigVGAN custom CUDA kernel for alias-free activation is optional; fallback performance impact unquantified.
- **Semantic Codec State Dict Mismatch:** Both call load_state_dict(strict=False), allowing partial weight loading — silent divergence risk if checkpoint format changes.
- **CAMPPlus Speaker Embedding:** vLLM loads from campplus.pth if available, else uses uninitialized default. Official always requires valid weights.
- **Text Tokenizer State:** TextNormalizer requires glossary.yaml and bpe.model in model_dir with no load-time validation — fails downstream without clear error trace.
- **Streaming Disabled:** Pipeline explicitly sets async_chunk=false; S2Mel cannot stream partial audio. Must be documented for production use.

## Alternative Directions Considered

### Alt-1: Colab L4 Test Harness Design
- Gist: Design a production-ready Colab notebook with L4 GPU (authuser=3), dual-mode inference (offline via IndexTTS class + online via vLLM OpenAI API), phased cell execution, and structured output to /content/index-tts-outputs/. Leverages existing test patterns from regression_test.py, end2end.py, and speech_client.py with MCP Colab tools for GPU/auth control.
- Why not primary: Depends on code alignment being correct first — a test harness validates but doesn't fix implementation gaps.

### Alt-2: ASR Validation Methodology
- Gist: Leverage the existing Seed-TTS evaluation framework (seed_tts_eval.py, ~752 lines) for dual-model ASR: Whisper-large-v3 for English, funasr Paraformer-zh for Chinese. Compute WER via jiwer, SIM via WavLM, UTMOS via TorchScript JIT.
- Why not primary: ASR validation is a downstream consumer — metrics are meaningless if model diverges from reference.

### Alt-3: Staged Progress Instrumentation
- Gist: Extend ProgressBarMixin and _set_gr_progress callback into a general stage logging system with per-stage timing and intermediate output.
- Why not primary: Additive (flag-guarded logging) — valuable for debugging but doesn't affect correctness.

### Alt-4: Offline vs Online Mode Divergence
- Gist: Document architectural separation between sync Omni.generate() and async /v1/audio/speech. Key: async_chunk=false, SharedMemoryConnector, input validation differences.
- Why not primary: Mode divergence is by design — understanding it informs testing but doesn't drive alignment.

### Alt-5: Output Organization and Reproducibility
- Gist: Structured output with timestamp dirs, output_{id}.wav naming, manifest.json metadata, following bench_tts.py conventions.
- Why not primary: Supporting concern — important for reproducibility but secondary to correct implementation.

## Synthesis Notes

The primary direction (gap analysis) establishes the foundation: without verified code-model alignment, all downstream testing is unreliable. However, elements from every alternative fold into the final plan. Alt-1's Colab harness becomes the execution vehicle. Alt-2's Seed-TTS eval framework provides ASR metrics. Alt-3's staged progress instrumentation addresses the user's explicit request for intermediate output. Alt-4's offline/online mode understanding ensures both test paths are architecturally correct. Alt-5's output conventions give Colab outputs a reproducible structure.

--- Original Design Draft End ---
